# Base URL of the compliance-auditor backend (api.py). Defaults to the
# deployed Render instance if unset — override for local dev, e.g.:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
