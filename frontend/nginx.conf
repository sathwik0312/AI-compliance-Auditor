server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router uses client-side routing (/, /dashboard) — fall back to
    # index.html for any path that isn't a real static file.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
